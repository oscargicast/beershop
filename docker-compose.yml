services:
  app:
    image: beershop
    build:
      context: .
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    volumes:
      - ./src:/code/src
      - ./tests:/code/tests
      - .:/code
    env_file:
      - .env
    ports:
      - "8000:8000"